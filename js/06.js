//Objeto Math

let resultado; //pedir resultado

resultado = Math.PI; //mostrar número PI
resultado = Math.round(2.9); // Nos redondeé el resultado
resultado = Math.ceil(2.9); // Nos redondeé el resultado hacia arriba
resultado = Math.floor(2.9); // Nos redondeé el resultado hacia abajo
resultado = Math.sqrt(144);  // raíz Cuadrada
resultado = Math.abs(200); // número opuesto
resultado = Math.min(3, 5, 1, 8, 2, 10); // Mostrar número mínimo
resultado = Math.max(3, 5, 1, 8, 2, 10); // Mostrar número máximo
resultado = Math.random(); 
resultado = Math.floor(2.9); // Nos redondeé el resultado hacia abajo
resultado = Math.floor(Math.random() * 30); //Mostrar un número exacto

console.log(resultado); //que nos imprima el resultado

